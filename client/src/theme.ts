import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  styles: {
    global: {
      body: {
        fontFamily: "'MierB', sans-serif",
      },
    },
  },
});

export default theme;
